# ============================================
# Auto-Updating 485 Visa Intelligence System
# Runtime dependencies for the deployed Streamlit app.
#
# Streamlit Cloud installs this file, so it holds only what serving a
# question needs. Scraping, PDF ingestion and dev tooling live in
# requirements-dev.txt — they are needed to BUILD the index, not to query
# one, and shipping them to the app would waste a constrained container.
# ============================================

# ---- Retrieval ----
chromadb>=0.5.0
sentence-transformers>=3.0.0
transformers>=4.44.0

# ---- LLM (Groq - free, instant) ----
groq>=0.10.0
# Optional: only needed if you set OPENAI_API_KEY and use the OpenAI provider
# in src/generation/llm_client.py. Uncomment to enable.
# openai>=1.40.0

# ---- UI ----
streamlit>=1.40.0
plotly>=5.18.0
pandas>=2.1.0

# ---- Configuration & data ----
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0

# ---- Quality & Production ----
loguru>=0.7.0
tenacity>=8.2.0
