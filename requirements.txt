# ============================================
# Auto-Updating 485 Visa Intelligence System
# Python Dependencies
# ============================================

# ---- Core RAG ----
# Only the text splitter is used directly (src/ingestion/text_chunker.py);
# the full langchain metapackage is not needed.
langchain-text-splitters>=0.3.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
transformers>=4.44.0

# ---- PDF Processing ----
pypdf>=5.0.0
pdfplumber>=0.11.0

# ---- LLM (Groq - free, instant) ----
groq>=0.10.0
# Optional: only needed if you set OPENAI_API_KEY and use the OpenAI provider
# in src/generation/llm_client.py. Uncomment to enable.
# openai>=1.40.0

# ---- UI ----
streamlit>=1.40.0
plotly>=5.18.0

# ---- Web Scraping ----
beautifulsoup4>=4.12.0
lxml>=5.0.0
httpx>=0.27.0
# Browser-impersonating fetch backend (src/scraping/fetchers.py). The
# [fetchers] extra pulls in curl_cffi, which the base package omits.
# Optional: the scraper falls back to httpx if this is not installed.
scrapling[fetchers]>=0.4.14

# ---- Configuration ----
python-dotenv>=1.0.0

# ---- Data & Validation ----
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
pandas>=2.1.0

# ---- Quality & Production ----
loguru>=0.7.0
tenacity>=8.2.0

# ---- Dev / test ----
pytest>=8.0.0
ruff>=0.6.0
fpdf2>=2.7.0
